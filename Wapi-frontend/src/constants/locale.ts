/** Default UI language when nothing is stored yet (new visitors / cleared storage). */
export const DEFAULT_LOCALE = "ar";

/** Bundled fallback when a locale has no resources. */
export const I18N_FALLBACK_LNG = "en";
